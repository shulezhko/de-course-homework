# Скопіюйте у .env (docker compose читає його автоматично).
# AIRFLOW_UID — uid, від якого працюють контейнери Airflow. На macOS лишайте 50000.
# На Linux підставте свій: echo "AIRFLOW_UID=$(id -u)" > .env
AIRFLOW_UID=50000
